[build]
  functions = "netlify/functions"

# Proxy: /api/apply → Netlify Function
[[redirects]]
  from   = "/api/apply"
  to     = "/.netlify/functions/submit-application"
  status = 200
  force  = true
